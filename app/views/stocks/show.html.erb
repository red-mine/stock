<h1><%= @stock.upcase %></h1>

<h2>Boll'in Bands</h2>

<%= line_chart @stocks.map {
  |stock| {
    name: stock[:name], 
    data: stock[:data]
  }
} %>

<h2>Lohas Staves</h2>

<%= line_chart @staves.map {
  |stave| {
    name: stave[:name], 
    data: stave[:data]
  }
} %>